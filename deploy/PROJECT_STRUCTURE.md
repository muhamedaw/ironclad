# Project structure (from this build session)

ironclad/
├── frontend/          React + Vite (marketplace, page builder, checkout, admin)
├── api/               Node.js Express (products, orders, auth, recommendations)
├── checkout-service/  Node.js Express (payment processing)
└── db/                MySQL schema + migrations
